# Data Layout

- `raw/`: timestamped API payloads for replay and debugging.
- `processed/`: optional Parquet exports and derived datasets.
- `mlb_markets.duckdb`: local analytical database.

Raw and processed snapshots are intentionally ignored by Git.
